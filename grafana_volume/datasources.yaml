apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    #docker inspect -f "{{ .NetworkSettings.IPAddress }}" localprometheus or socket.gethostbyname(socket.gethostname())
    url: http://prometheus:9090